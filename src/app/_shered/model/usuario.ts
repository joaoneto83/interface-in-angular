// import { Dependente } from './dependente';
// import { Arquivo } from '../../_models/Arquivo';

export interface Usuario {
      id: string,
      nome: string,
      email: string,
      avatar: string,
      perfilId: number,
      perfil: string,
     

    totalRegistros: null

}; 